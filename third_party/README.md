# third_party

Place the `velocity_metrics` package (OceanDataLab, LGPL-3, currently vendored in
`NOSC/env/velocity_metrics`) here and `pip install -e third_party/velocity_metrics` to enable
`lagrangian_drifters` with `backend: velocity_metrics`. Keep its license file.
