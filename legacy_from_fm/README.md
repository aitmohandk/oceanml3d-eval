# Evaluation code imported from 4dvarnet-fm-opencode, awaiting conversion

Copied verbatim from the upstream repository (`evaluation/` plus the root `eval_*.py` /
`evaluate_all*.py` drivers). **Nothing here is maintained**: each file stays until the corresponding
benchmark in `benchmarks/` reproduces its numbers, then it is deleted in the same commit that proves
the replacement (strangler fig). Do not import from this directory in new code.

Conversion status is tracked in `PLAN.md`. The upstream repository itself is never modified.
